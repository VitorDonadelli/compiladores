package br.ufscar.dc.compiladores.findinator.contexts;

// Classe para os Contexts que são testes 
// obs: não precisava, acabou que todos os contexts ficaram com build
public abstract class MyTesteContext extends MyContext {
    public abstract String build();
}
